<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.commit.mappers.ProfileMapper">

	<!-- 신상정보 select문 -->
	<select id="profileSelect" parameterType="MypageVo" resultType="MapageVo">
	SELECT 
		p.weight,
		p.height,
	    p.mbti, 
	    p.hobby, 
	    p.photo, 
	    p.military, 
	    p.religion, 
	    p.wealth, 
	    p.marriage, 
	    p.disease
	FROM user u
	JOIN profile p ON u.id = p.user_id
	WHERE u.id = #{userId}
	</select>
	 
</mapper>	